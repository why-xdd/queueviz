{
  "name": "queueviz",
  "version": "0.1.0",
  "private": true,
  "description": "Watch queues, backpressure and scheduling behave — an interactive simulation of the things that decide your tail latency.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
